# project-init: record of choices made at init time.
# Safe to hand-edit; re-running `project-init` will reconcile with this file.

project:
  name: "example-python"
  description: "Example scaffolded Python project"
  created: 2026-04-26
  project_init_version: 0.1.0

language: python            # python | node | go | none

memory:
  stack: obsidian-only         # obsidian-only | obsidian-lightrag
  vault_path: .claude/vault
  memory_path: .claude/memory

mcps:
  installed: ["linear", "github", "context7"]

tooling:
  lint_command: "uv run ruff check ."
  format_command: "uv run ruff format ."
  test_command: "uv run pytest"
