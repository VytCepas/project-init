# `.claude/commands/`

Project-local Claude Code slash commands. Drop markdown files here; they become `/<filename>` inside a Claude Code session.

See the [Claude Code slash commands docs](https://docs.claude.com/en/docs/claude-code/slash-commands).

User-level commands (available in every project) live in `~/.claude/commands/`.
