# Project: {{project_name}}

> {{project_description}}

Scaffolded with [project-init]({{project_init_url}}) on {{created_date}}.

| | |
|---|---|
| Language | {{language}} |
| Memory stack | {{memory_stack}} |
| MCPs | {{installed_mcps}} |

## Workflow

1. **Start of session** — read [`memory/MEMORY.md`](memory/MEMORY.md), then check [`docs/adr/`](docs/adr/) for relevant decisions.
2. **During work** — permanent decisions → [`docs/adr/`](docs/adr/); exploratory notes → [`vault/`](vault/); reusable facts → [`memory/`](memory/).
3. **End of session** — `session-end` hook writes a session log to [`vault/sessions/`](vault/sessions/){{#if lightrag}}, LightRAG ingester indexes memory{{/if}}.

## Tools

| Type | Name | Purpose |
|---|---|---|
| Command | `/status` | git state, recent commits, open TODOs |
| Command | `/review` | code review of staged changes or a file |
| Command | `/plan <task>` | acceptance tests first, then implementation plan |
| Command | `/request-review` | mark PR ready, move board card, optional agent review |
| Skill | `start-task` | create issue + branch + draft PR before starting work |
| Skill | `session-summary` | summarise session and save to vault |
| Skill | `add-hook` | add a new deterministic hook |
| Skill | `add-command` | add a new slash command |
| Agent | `reviewer` | code review specialist |
| Agent | `researcher` | codebase explorer |

Hooks run automatically via `settings.json`: `bash-safety-guard`, `pre-commit-gate`, `post-edit-lint`{{#if obsidian}}, `session-end`{{/if}}.

**Rules** (`.claude/rules/`): per-filetype conventions loaded automatically by Claude Code when you open a matching file.

## Coding standards

- No comments unless the WHY is non-obvious.
- No premature abstractions — three similar lines before extracting.
- No error handling for impossible scenarios.
- No backwards-compatibility shims — delete removed code.
- Prefer editing existing files over creating new ones.
{{#if lint_command}}- `{{lint_command}}` must pass before closing a task.
{{/if}}

## TDD

Write failing tests before implementation. Tests define the contract.

1. `/plan <task>` → acceptance tests (red)
2. Commit failing tests
3. Implement until green
4. Lint and clean up
{{#if python}}
Test conventions: one assertion per test, name as `test_<unit>_<scenario>`, run with `uv run pytest`. Real DB/API instances — no mocks.
{{/if}}

## GitHub Projects tracking

Work items are GitHub Issues; the project board is GitHub Projects (kanban with To Do / In Progress / In Review / Done columns). Board cards move automatically via `board-automation.yml`.

### Lifecycle: Issue → branch → draft PR → merge

Every non-trivial task follows this exact sequence:

| Step | Action | Board column |
|------|--------|-------------|
| 1 | Run `/start-task` — creates issue, branch, draft PR | **To Do → In Progress** |
| 2 | Implement on the branch; commit with `#<issue>` references | In Progress |
| 3 | Run `/request-review` — marks PR ready, optional reviewer agent | **In Progress → In Review** |
| 4 | Tests pass on CI; PR merged with `Closes #<issue>` in body | **In Review → Done** |

### Rules

- **No direct commits to `main`** — all changes through PRs only. Install the pre-push hook locally:
  ```bash
  cp .github/hooks/pre-push .git/hooks/pre-push && chmod +x .git/hooks/pre-push
  ```
- One issue → one branch → one PR.
- Branch name: `<issue-number>-<kebab-slug>` (e.g. `42-add-auth`).
- **PR title:** `[#<issue>][<type>] Short description` where type ∈ {feat, fix, chore, docs, test}
  - For small no-issue PRs: `[nojira][type] Short description` (e.g. `[nojira][fix] Fix typo`)
- PR body must include `Closes #<issue>` — auto-closes issue and moves board card on merge (skip for nojira PRs).
- Draft PRs created immediately when work starts — not when it's done.
- PRs must pass CI (tests + lint) before merging.

### PR Types

| Type | Use case | Example |
|---|---|---|
| `feat` | New feature or enhancement | `[#42][feat] Add OAuth login` |
| `fix` | Bug fix | `[#99][fix] Handle null pointer` |
| `chore` | Maintenance, refactor, deps, CI | `[#16][chore] Update dependencies` |
| `docs` | Documentation-only change | `[#20][docs] Update API guide` |
| `test` | Test-only change | `[#55][test] Add unit tests` |

### Code review (optional)

`/request-review` asks whether to run the `reviewer` agent (~500 extra tokens). Skip for trivial changes; use for anything security-sensitive or architecturally significant.

### Quick reference

```bash
gh issue list                    # see open issues (board backlog)
gh issue create                  # create a new issue (adds to board)
gh pr list                       # see open PRs
gh pr view                       # current branch's PR status
gh pr checks                     # CI status for current PR
gh project list --owner <owner>  # list GitHub Projects boards
```

## Conventions

- Hooks and scripts prefer bash/python. LLM calls only for generative steps.
- Everything under `.claude/`. Root only has `CLAUDE.md`/`AGENTS.md` redirects.

## Compact Instructions

When compacting, preserve:
- GitHub Issue number being worked on
- Files modified this session
- Test results (pass/fail count, failing test names)
- Unresolved errors or lint failures
- Decisions not yet committed to `docs/adr/`
