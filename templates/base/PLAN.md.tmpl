# Plan — {{project_name}}

Done when: TODO — the one outcome that makes this project finished, stated so a reader can check it.

probed by: TODO — the command or check that proves it; it must fail until the outcome holds.

The two lines above are this project's done-gate. "Is this finished?" is
answered by running the probe, never by ticking a box. Replace both TODOs
before the first milestone, and change them only when the goal itself changes.
