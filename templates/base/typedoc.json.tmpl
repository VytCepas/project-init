{{#if node}}{{#if want_docs}}{
  // typedoc.json — local API-documentation preview config (scaffolded by project-init).
  // TypeDoc accepts JSONC. Local build:  bunx typedoc
  // Local-only: no publish workflow ships (the GitHub Pages docs.yml was retired — PI-343/ADR-004).
  "entryPoints": ["src/index.ts"],
  "out": "_site",
  "excludePrivate": true,
  "validation": {
    "notExported": true,
    "invalidLink": true,
    "notDocumented": true
  }
}
{{/if want_docs}}{{/if node}}