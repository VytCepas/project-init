# Code map

Generated by `.agents/scripts/gen_code_map.py` — what each module does,
at a glance. **Read this before grepping the tree.** Regenerate after
public-API changes (`just code-map`). Do not hand-edit.

### `project_init/__init__.py`

project-init — scaffolder for agentic-development infrastructure.


### `project_init/__main__.py`

CLI entry point for `project-init` and `uvx project-init`.

- `class ScaffoldInputs` — The resolved wizard inputs as one named record (PI-190).
- `def resolve_delivery` — Normalize a delivery value; default 'prototype'.
- `def resolve_deploy` — Normalize a deploy target; default 'none'.
- `def resolve_iac` — Normalize an --iac value; default 'none'. Raises ValueError on an unknown tool.
- `def resolve_agents` — Parse/validate an --agents value; claude is always included first.
- `def agent_layers` — Template layers contributed by the selected agents (no fallback).
- `def render_run_command` — Fill ``{project_slug}`` in a language ``run_command``.
- `def main` — Run the scaffolding CLI; return the process exit code.

### `project_init/capabilities.py`

Deterministic capabilities inventory (PI-374, ADR-017).

- `def canonical_skills` — Return (name, description) for every skill the scaffold ships.
- `def canonical_hooks` — (event, script) pairs for the always-on hooks.
- `def surface_hooks` — (hook file, events) for the GUI surfaces selected via --agents (#366).
- `def render` — The CAPABILITIES.md content for a scaffold described by *variables*.
- `def emit` — Write .agents/CAPABILITIES.md.

### `project_init/concerns.py`

``project-init add`` / ``remove`` — toggle a concern on an existing scaffold.

- `class ConcernError` — A bad concern name or value supplied to add/remove.
- `def apply_concern` — Toggle *concern* on the scaffold at *target*; return a process exit code.

### `project_init/console.py`

Central rich styling for the interactive wizard.

- `def is_interactive` — Whether richer devices should render (True only on a real TTY).
- `def option_line` — One numbered menu row in the single house style.
- `def render_presets` — Print the preset options as an aligned table (plain list off-TTY).
- `def scaffolding` — Spinner for the duration of a long, single-shot operation.

### `project_init/governance.py`

Deterministic AIBOM generation for the governance overlay (ADR-018, #412).

- `def extract_ccr_routes` — Parse a claude-code-router ``config.json`` into detected routes.
- `def render_aibom` — Build the AIBOM markdown from the recorded selection + emitted overlays.
- `def emit` — Write ``.agents/governance/ai-bom.generated.md``.

### `project_init/mcps.py`

MCP catalog and emit helpers for the project-init wizard.

- `def servers_for_ids` — Canonical MCP server specs for the given catalog ids.
- `def format_installed_mcps` — Human-readable comma-separated list for template substitution.
- `def format_installed_mcps_yaml` — Inline YAML list string for config.yaml template.

### `project_init/migration_notes.py`

Curated per-version upgrade notes surfaced by `project-init upgrade` (#244).

- `def notes_for_span` — Return ``[(version, entry)]`` for the span, newest version first.

### `project_init/scaffold.py`

Core scaffolding logic — pure functions, no user interaction.

- `class TemplateRenderError` — Raised in strict mode when unrendered placeholders survive scaffolding.
- `def list_presets` — Return all available presets as parsed dicts, sorted by name.
- `def parse_version` — Parse a leading ``X.Y.Z`` (optional ``v`` prefix) into a tuple, or None.
- `def load_preset` — Load a preset by name, resolving ``extends`` inheritance (#252).
- `def generate_preset` — Write a starter company preset that extends *extends* (#252).
- `def memory_tier` — Descriptor tier number for *memory_stack* (#498, ADR-024).
- `def memory_layers` — Template layers contributed by the memory backend (#466, #497).
- `def overlay_layers` — Extra template layers appended to a preset beyond its base definition.
- `def marketplace_source_vars` — Host-aware plugin-marketplace source fields for settings.json (ADR-013, #248).
- `def slugify` — Kebab-case *name* for identifier-ish template slots (deploy app names).
- `def read_preserve_globs` — User-declared extra preserve globs from ``.agents/config.yaml`` (#243).
- `def hash_bytes` — Content hash used by the scaffold-record manifest (shared with upgrade).
- `def scaffold` — Copy + render template layers into *target*. Return created file paths.

### `project_init/surfaces.py`

Per-surface agent-config generation (ADR-017, PI-366).

- `def mcp_server_specs` — Canonical {name: {command,args}|{url}} from selected catalog entries.
- `def render_mcp_json` — MCP config as JSON.
- `def render_antigravity_mcp` — MCP config for Antigravity's ``.agents/mcp_config.json``.
- `def render_mcp_toml` — MCP config as Codex ``config.toml`` ``[mcp_servers.<name>]`` tables.
- `def render_cursor_hooks` — Render `.cursor/hooks.json` (version 1).
- `def render_antigravity_hooks` — Render `.agents/hooks.json` for Antigravity's ``safety-gate`` model.
- `def render_mcp_for` — Render MCP config for a ``(format, key[, drop_type])`` spec.
- `def surface_files` — All files to write for a selected *surface*: {relpath: content}.
- `def planned_files` — Map a selection to ``{relpath: content}``.
- `def emit` — Write the per-surface config files into *target*.

### `project_init/upgrade.py`

`project-init upgrade` — re-render from recorded config with a drift report.

- `class UpgradeError` — Raised when the project cannot be upgraded (missing/invalid config).
- `class DriftReport` — Outcome of comparing a fresh render against the scaffolded project.
- `def read_base` — Return the recorded ``{rel: rendered-text}`` merge base, or ``{}``.
- `def write_base` — Persist the ``{rel: rendered-text}`` merge base sidecar (sorted, pretty).
- `def write_scaffold_record` — Append/replace the scaffold record block in .agents/config.yaml.
- `def read_scaffold_record` — Return (preset_name, variables, manifest, migrated) for *target*.
- `def read_recorded_manifest` — Recorded content hashes for *target*, ``{}`` when absent or unreadable.
- `def compute_drift` — Compare the staged re-render against the project tree.
- `def apply_drift` — Apply changes; 3-way-merge user edits (#240); conflicts become ``.new``.
- `def run_upgrade` — Entry point for the upgrade subcommand; returns a process exit code.
